version: '3'

services:

  codellama:
    build: .
    tty: true
    volumes:
      - .:/root
    deploy:
      resources:
        reservations:
          devices:
            - driver: nvidia
              count: 1
              capabilities: [gpu]

